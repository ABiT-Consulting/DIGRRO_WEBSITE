export const stripeCheckoutMode = 'server';
export const isStripeConfigured = true;

export function getStripeCheckoutLabel() {
  return 'Continue to Stripe';
}

export function getStripeCheckoutSummary() {
  return 'Choose a plan, complete your registration details, and continue to secure Stripe checkout.';
}
